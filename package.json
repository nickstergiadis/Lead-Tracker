{
  "name": "restore-at-home-lead-tracker",
  "private": true,
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "test": "node --test",
    "lint": "eslint app.js business test scripts eslint.config.mjs",
    "check": "for file in app.js business/*.mjs scripts/*.mjs test/*.mjs; do node --check \"$file\" || exit; done",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "eslint": "^9.32.0"
  },
  "type": "module"
}
